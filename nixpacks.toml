[phases.setup]
nixPkgs = ["php83", "php83Extensions.sqlite3", "php83Extensions.curl", "php83Extensions.pdo_sqlite"]

[phases.install]
cmds = ["mkdir -p /app/data"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t . router.php"
